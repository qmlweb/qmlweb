//Generated -- remove this comment to prevent it from being overwritten
describe('QtQuick.RegExpValidator', function() {
  var loader = prefixedQmlLoader('QtQuick/qml/RegExpValidator');
  it('can be loaded', function() {
    var div = loader('Empty')
    div.remove()
  })
})
